# Mission Loop — `Mission`, `MissionStore`, supervisor e `pipe mission`

O Mission Loop transforma uma missão confirmada pelo fundador em trabalho delegado, sem um LLM orquestrador conversando: um **supervisor determinístico em Python** despacha um worker Claude Code headless (`claude -p`) por ciclo num worktree dedicado, verifica o diff de forma mecânica, chama um **revisor de contexto limpo** (só leitura, veredito por JSON schema), roteia, abre o PR e só conclui com evidência de todos os critérios.

- Ticket A (PIP-901): contrato `Mission`, `MissionStore` (SQLite, eventos hash-chained), estados, decisões humanas, evidência e o CLI de estado.
- Ticket B (PIP-902): `worker.py`, `verify.py`, `reviewer.py`, `delivery.py`, `supervisor.py`, `pipe mission supervise|run-once|reconcile` e a skill `/missao` (`.claude/skills/missao/SKILL.md`).
- Ticket C (PIP-903): contrato v0.2.0 com `delegation`, `resolve_decision` com a fonte `delegated:orchestrator`, o supervisor concedendo `grant_cycle` sozinho quando a regra cobre, retry único do revisor em falha de infraestrutura e `delegatedDecisions` em `status`.
- Ticket D (PIP-906): segunda regra de delegação, `delegation.answerBlockers`, e o **respondedor**: um `claude -p` somente leitura, isolado como o worker e o revisor, que tenta responder um bloqueio técnico do worker antes de acordar o fundador. Ver "Decisões delegadas" e "O respondedor de bloqueios" abaixo.
- PIP-906 v2 (correção da revisão adversarial do PR #193): guarda de termos sensíveis normalizada (falhava com acento, separador `_-./` ou caractere de largura zero), pausa/cancelamento durante o respondedor sem derrubar o supervisor, bloqueios cercados no prompt do respondedor, write set checado antes de chamar o respondedor, e `Read` de `~/.ssh`/`~/.claude`/`.env*` negado ao respondedor.
- PIP-905 (ambiente limpo para todo filho do supervisor): `child_env()` agora remove também toda variável que `git rev-parse --local-env-vars` lista (derivada do próprio git, não copiada à mão) e as de config por ambiente `GIT_CONFIG_PARAMETERS`/`GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>`, mais `PYTHONNOUSERSITE`, `PYTHONPLATLIBDIR`, `PYTHONSAFEPATH`, `VIRTUAL_ENV` e `__PYVENV_LAUNCHER__` do interpretador; `gh_env()` (usado por todo `gh` do supervisor) é `child_env()` mais `core.hooksPath=/dev/null` forçado via `GIT_CONFIG_*`, porque o `gh` roda `git` por dentro. Ver "Entrega sem hooks e sem vazamento de `GIT_*`" abaixo.
- PIP-911 onda 1 (executor no contrato e quem rodou gravado no run): `execution.executor` (`claude`/`local`) entra no contrato do `Program` (`schemas/Program.schema.json`, `program.py`) ao lado de `workerModel`/`reviewerModel`; uma onda com critério `rubric` recusa `executor: "local"` na validação (o revisor, que julga o `rubric`, não tem campo de executor e nunca é local). `mission_runs` ganha `executor_kind`/`model` como colunas próprias (banco existente migra sem perder linha). Ver "`execution` ({...})" e "Store" abaixo. Roteamento de verdade para um executor local continua fora deste ticket (onda 2).

Desenho de origem: `10-desenho-mission-loop-mvp.md` (D1–D11; §3 contrato; §4 tabelas; §5 falhas; §7 missão-demo).

## O que é uma Mission

Um documento `Mission` v0.1.0 ou v0.2.0 (`schemas/Mission.schema.json`; contrato imposto em `src/pipe_venture_builder/mission/contract.py`) com:

- `missionId` `MSN-<12hex>`, gerado por `stable_id` só a partir do conteúdo (recriar o mesmo JSON é idempotente; missão nova muda conteúdo ou `version`); `fingerprint` cobre tudo menos `status`, `createdAt`, `updatedAt`, `fingerprint`.
- `successCriteria`: cada critério tem forma verificável — `check` (`command`, `cwd` opcional), `artifact` (`path`, `mustMatch` opcional) ou `rubric` (`question`). Critério sem forma é recusado.
- `delegation` (opcional; a chave fica **ausente** num documento v0.1.0 — não entra na fingerprint, então o mesmo JSON v0.1.0 continua gerando o mesmo `missionId` de antes do PIP-903 — e é `null` ou uma ou duas regras numa missão v0.2.0): quando presente e não nula, exige `schemaVersion: "0.2.0"` e um objeto com pelo menos uma das duas chaves — `grantCycle: {"maxTimes": 1-3, "maxCostFraction": (0, 0.8], "requireProgress": bool}` e/ou `answerBlockers: {"maxTimes": 1-3}` — sozinha ou as duas juntas; qualquer outra chave, no nível de `delegation` ou dentro de cada regra, é recusada. Ver "Decisões delegadas" abaixo.
- `constraints`: `maxCycles`, `maxBudgetUsd`, `maxTurnsPerRun` positivos; `productionAllowed`, `secretsAllowed`, `externalCommsAllowed`, `billingAllowed` **têm de ser `false`**. Gates absolutos não são escalados: são recusados na criação.
- `workspace.repo` absoluto, `baseRef`, `writeSet` relativo, não vazio, sem `..`.
- `delivery.kind ∈ {none, pull_request}` e `requireChecks`.
- O documento inteiro passa por `payload_is_safe`; sentinelas secret-shaped são recusadas com mensagem fixa, sem eco do valor.

## Estados e transições

`draft → active`; `active ↔ paused`; `active|paused|blocked → cancelled`; `active → blocked`; `blocked → active` (só sem decisão pendente); `active → completed`; `active|paused|blocked → unknown` **só** por `mark_unknown` (reconciliação). Transição inválida levanta `ControlPlaneStateError` e nada é persistido. `completed`, `cancelled` e `unknown` são terminais.

`complete` exige: cadeia de eventos válida, evidência `satisfied=true` mais recente para **todos** os critérios, nenhuma decisão pendente e, quando `delivery.kind = pull_request`, evento `delivery.pr_opened` (e, se `requireChecks`, o **último** evento `delivery.checks_*` após o último `pr_opened` tem de ser `checks_passed` — uma falha posterior ou um novo `pr_opened` volta a bloquear).

## Eventos

Allowlist fixa em `events.py` (`mission.*`, `run.*`, `verify.*`, `review.*`, `decision.*`, `delivery.*`, `budget.reached`). Cada evento traz `sequence`, `previousHash` e `eventHash = fingerprint(evento sem o hash)`. O payload aceita só identificadores, hashes, contagens, estados e refs: toda string passa por `safe_identifier`, então texto livre, prompt, saída de conversa ou diff não entram. `verify_chain(mission_id)` recomputa a cadeia inteira; `status` expõe `auditChainValid`.

## Decisões humanas

`open_decision(kind ∈ {approval, clarification, escalation, out_of_mission, budget}, context curto, options, safe_default, blocked_scope, deadline)`; pedido pendente idêntico não duplica. `resolve_decision(decision_id, option, decided_by)` exige opção válida e `decided_by` com prefixo `human:chat:`, `human:linear:` ou `human:cli:`, **ou** exatamente `delegated:orchestrator` (ver "Decisões delegadas") — qualquer outra fonte (agente, supervisor por conta própria) é recusada. Silêncio nunca aprova; o `safe_default` é informação para quem decide, não uma resolução automática.

### Decisões delegadas

O fundador pode declarar, na própria missão, um limite dentro do qual o supervisor resolve sozinho um pedido rotineiro. Há duas regras independentes, cada uma cobrindo só a sua decisão: `delegation.grantCycle` (conceder um ciclo além de `maxCycles`) e `delegation.answerBlockers` (responder um bloqueio técnico do worker — PIP-906). Uma missão pode declarar uma, a outra, ou as duas. Nada além disso é delegável hoje — nunca merge, produção, segredos, comunicação externa, billing, orçamento (`kind: budget`) ou um veredito `out_of_mission`.

**`grantCycle`** — só dois motivos são delegáveis: bater o limite de ciclos, sozinho (`max_cycles`) ou depois de esgotar as revisões (`needs_revision_limit`). Qualquer outro motivo que hoje abre uma decisão `escalation`/`grant_cycle` — um veredito `blocked` legítimo do revisor (`review_blocked`), o circuito anti-loop (`no_progress`), o worker falhando até o último ciclo (`run_failed`) ou os checks de entrega falhando no último ciclo (`delivery_checks_failed`) — **sempre** fica pendente para o fundador, mesmo com uma regra de delegação ativa na missão: o supervisor só tenta a delegação quando o motivo é um dos dois acima, e o store recusa a fonte `delegated:orchestrator` para qualquer outro motivo, gravado em `context.reason` da própria decisão.

- `resolve_decision(decision_id, option="grant_cycle", decided_by="delegated:orchestrator")` só é aceito quando a decisão é `kind: escalation` com a opção `grant_cycle`, `context.reason ∈ {max_cycles, needs_revision_limit}`, a missão tem `delegation.grantCycle`, o número de concessões já feitas (`decision.delegated` com `rule: grantCycle`) está abaixo de `maxTimes`, e o custo acumulado dividido por `maxBudgetUsd` está dentro de `maxCostFraction` (igual ao limite concede; só acima dele recusa). Qualquer outra combinação (opção, `kind`, motivo, regra ausente, limite estourado) é recusada com `ControlPlaneContractError`/`ControlPlaneStateError` — o mesmo par de exceções que qualquer outra violação de contrato ou de estado.
- Quando aceito, grava o mesmo jeito que qualquer decisão (linha em `decisions`, `status: resolved`), mas o evento é `decision.delegated` (payload `decisionId` + `rule: "grantCycle"`), nunca `decision.resolved` — assim `_max_cycles()` (que já soma decisões resolvidas com `grant_cycle`) e o resto do código continuam sem saber a diferença entre uma concessão humana e uma delegada.
- `requireProgress` (o ciclo que terminou satisfez mais critérios que o anterior; o primeiro compara com zero) é um julgamento por ciclo que só o supervisor tem contexto para fazer — o store não o verifica; é o supervisor que decide se tenta a delegação antes de chamar `resolve_decision`.
- O supervisor só tenta a delegação em `max_cycles` e `needs_revision_limit` — nunca em `no_progress`, `review_blocked`, `run_failed`, `delivery_checks_failed`, nos casos com outras opções (`branch_mismatch`, `delivery_outside_write_set`, `git_config_tampered`, checks sem resposta) nem em `budget`. Se a regra cobre o caso, resolve a decisão como `delegated:orchestrator`, chama `resume()` e o ciclo seguinte já sai sem pedir nada ao fundador; senão, a decisão fica pendente. Missões sem `delegation` — inclusive toda missão v0.1.0, onde a chave é sempre ausente — se comportam de forma idêntica ao PIP-902.

**`answerBlockers`** (PIP-906) — o único motivo delegável é `worker_blockers`: o worker devolveu `done: false` com `blockers`. Ver "O respondedor de bloqueios" abaixo para o fluxo completo.

- `resolve_decision(decision_id, option="retry", decided_by="delegated:orchestrator")` só é aceito quando a decisão é `kind: clarification` com a opção `retry`, `context.reason == "worker_blockers"`, a missão tem `delegation.answerBlockers`, e o número de concessões já feitas (`decision.delegated` com `rule: answerBlockers`) está abaixo de `maxTimes` (sem `maxCostFraction`: a regra só tem `maxTimes`). Qualquer outra combinação é recusada do mesmo jeito que `grantCycle`.
- Grava o mesmo jeito: `decision.delegated` com `rule: "answerBlockers"`, nunca `decision.resolved`.
- As duas regras nunca se confundem: um `grantCycle` sozinho na missão não cobre uma decisão `worker_blockers` (nem vice-versa) — cada `resolve_decision` delegado verifica `kind`+opção primeiro, depois a regra e o motivo certos para aquele par.

Em ambas as regras:

- A CLI (`pipe mission decide --by ...`) recusa qualquer `--by delegated:*` antes mesmo de chamar o store: é caminho interno do supervisor, nunca algo que se peça pela CLI (o texto de ajuda continua "as a named human").
- `status`/`build_status` expõe `delegatedDecisions`: a contagem de eventos `decision.delegated` da missão (soma das duas regras).

## O respondedor de bloqueios (PIP-906)

Hoje, todo `done: false` com `blockers` do worker pausa a missão e abre uma decisão `clarification` para o fundador — mesmo quando o bloqueio é técnico e a resposta já está no repositório ou na própria missão (ex.: "o worktree não tem `.venv`"). Com `delegation.answerBlockers` declarado, o supervisor tenta primeiro um **respondedor de contexto limpo** antes de acordar o fundador.

- **Quando roda**: só quando o worker termina um ciclo com `done: false` e `blockers` não vazio, o diff do worker **já verificado contra o write set** (abaixo) não tem nada fora dele, a missão declara `delegation.answerBlockers`, o texto dos `blockers` não bate a guarda de termos sensíveis (abaixo), e o número de respostas delegadas já dadas (`decision.delegated` com `rule: answerBlockers`) está abaixo de `maxTimes`. Fora isso — sem a regra, diff fora do write set, guarda disparada, ou limite esgotado — o respondedor nunca é chamado.
- **Write set antes do respondedor** (PIP-906 v2, achado 5): quando o respondedor rodaria, o supervisor primeiro roda o mesmo `changed_files`/`outside_write_set` da verificação normal sobre o worktree do ciclo. Se houver algo fora do write set — por exemplo um `.claude/settings.json` que o worker tenha deixado lá, cujos hooks rodariam no mesmo worktree do respondedor —, o respondedor nunca roda e o bloqueio **escala ao fundador** como antes do PIP-906 (a decisão `clarification` registra `outsideWriteSet` e o arquivo de revisão lista os arquivos a reverter). Missões sem a regra não passam por essa verificação aqui: seguem exatamente como antes (a verificação normal de write set acontece no ciclo seguinte).
- **O que é**: um `claude -p` no mesmo worktree do ciclo, com `--allowedTools "Read,Grep,Glob"` — **sem Edit, Write nem Bash**: ele não pode alterar um arquivo nem rodar um comando por conta própria. Isso não é o mesmo que "não executa código nenhum": ele ainda roda com `--setting-sources project` (como o worker e o revisor), então hooks de um `.claude/settings.json` legítimo do próprio repositório se aplicariam a ele como a qualquer um dos três — daí o passo anterior. Por cima do deny-list comum (`isolation_args()`), o respondedor nega adicionalmente `Read` de `~/.ssh/**`, `~/.claude/**`, `~/.aws/**`, `~/.config/gh/**`, `~/.netrc`, `~/.npmrc`, `~/.pypirc` e de qualquer `.env`/`.env.*` da máquina (`//**/.env`, ancorado na raiz — `**/.env` só valeria dentro do worktree) (`RESPONDER_DENIED_READS`, `responder.py`): é o único papel cuja saída (`instructions`) vira o brief do próximo worker sem passar pelo revisor, então uma leitura dessas nunca deveria voltar como resposta. `--permission-mode plan`, `--json-schema` com `{"action": "instruct"|"escalate", "instructions": str, "reason": str}`. O prompt traz a missão (intenção, critérios, não-objetivos, reservado ao fundador, write set) e os `blockers` do worker, cercados entre `<<<BLOQUEIOS_DO_WORKER` e `BLOQUEIOS_DO_WORKER>>>` e marcados como dado do worker, nunca instrução (`build_responder_prompt`): cada bloqueio vira uma string JSON numa linha só (quebras de linha escapadas), então nenhum bloqueio consegue pôr uma linha própria dentro da cerca — nem um marcador de fechamento falso (em qualquer caixa, espaçamento ou homóglifo) nem uma seção `## Instruções` falsa — nunca a transcrição do worker.
- **Portão estrutural (controle principal; PIP-906, 3ª revisão)**: a saída do respondedor traz, obrigatoriamente pelo schema e pelo parser, `category` (`environment`, `tooling`, `tests`, `codebase`, `mission_criteria`, `product`, `credentials`, `production`, `billing`, `customers`, `communication`, `governance`, `scope`, `other`) e `founderDecision` (bool). Só é aplicado `instruct` com uma das cinco categorias técnicas (`responder.DELEGABLE_CATEGORIES`) **e** `founderDecision: false`; qualquer outra combinação, ou a falta de um dos campos, escala. Por quê: três revisões adversariais mostraram que uma lista de palavras nunca fecha nos dois sentidos (deixa passar o que é do fundador e barra pergunta técnica); a lista permitida de temas técnicos é o que pode ser respondido sem o fundador, e o resto é dele por padrão. **O portão é autodeclarado pelo modelo**: nada liga `category` ao conteúdo do bloqueio. Medido uma vez com o CLI real (11/09, `--model haiku`): bloqueio "colocar a versao no ar e cobrar o cliente" → `escalate`, `category: production`, `founderDecision: true` (US$ 0,06); bloqueio "o worktree nao tem .venv, qual interpretador" → `instruct`, `category: tests`, `founderDecision: false` (US$ 0,14). Duas amostras não são uma taxa; as barreiras que **não** dependem do modelo são o sandbox do worker (sem ferramenta de merge, deploy, cobrança ou mensagem), o write set verificado antes de qualquer commit, o revisor de contexto limpo contra a intenção e os não-objetivos, e a CI.
- **`instruct` aplicado** (categoria técnica, `founderDecision: false`, `instructions` não vazio depois de removidos caracteres de controle, e nem os `blockers` nem as `instructions` batendo a guarda): a decisão `clarification` (`reason: worker_blockers`) é aberta e resolvida como `delegated:orchestrator` com a opção `retry` (evento `decision.delegated`, `rule: "answerBlockers"`) **com a missão ainda `active`** — o supervisor nunca pausa nem chama `resume` neste caminho, então uma pausa do fundador não pode ser desfeita —, e as instruções do respondedor viram o arquivo de revisão do ciclo (`revisions/cycle-<n>.md`, 0600) — o próximo worker as recebe no brief, do mesmo jeito que receberia uma instrução do revisor. A missão nunca fica com decisão pendente.
- **Guarda determinística (camada de reserva, não o controle principal)** (`guard.contains_sensitive_terms`, reexportada em `supervisor`; não depende do modelo): normaliza (Unicode NFKD sem acento, sem caractere de largura zero, homóglifos cirílicos comuns mapeados para latim, casefold), quebra em palavras (`STRIPE_API_KEY` vira `stripe api key`; camelCase começando em minúscula é separado, `stripeSecretKey` — nomes próprios como `GitHub` não; identificadores de código com afixo técnico, como `token_count`, `merge_dicts` e `hash_password`, são tratados como código e não como pedido; tokens com letra e número ganham também a leitura leet, `t0ken`; letras soltas em sequência são juntadas, `t o k e n`) e casa por **palavra inteira**, por **radical ancorado no início da palavra** (`merg` pega `mergear`, mas `cobr` nunca pega `descobrir`), por **pares de palavras próximas** (`chave … api`, `e-mail … cliente`) e por padrões de caminho/comando (`.env`, `~/.ssh`, `id_rsa`, `push -f`, nomes como `ANTHROPIC_KEY` em maiúsculas). Cobre: credencial/credential, segredo/secret, senha/password/passwd, token, API key/chave de API/SSH/privada/de acesso, PAT do GitHub, merge/mesclar/aprovar o PR, push forçado ou na main, produção/production/prod/deploy/go live/release para clientes/`railway up`/PyPI, billing/cobrança/cobrar/fatura/invoice/pagamento/payment/cartão, comunicação com cliente/usuário (e-mail/mensagem/Slack/WhatsApp/SMS/DM junto de cliente/usuário, ou um endereço de e-mail), governança (`AGENTS.md`, `CLAUDE.md`, `.pipe/mode.json`, operating modes, approval gates, `.claude/settings.json`, write set, "reservado ao fundador", sair/ampliar o escopo), `sudo` e `rm -rf`. Se os `blockers` **ou** as `instructions` baterem, a missão escala como hoje (`paused` + `clarification` pendente), mesmo que o respondedor diga `instruct`. **Limites conhecidos**: texto codificado (base64) e grafias deliberadamente trocadas fora dessas regras passam; a isenção de identificador vale só para palavras comuns em código (`merge`, `deploy`, `prod`, `mode`, `write`/`set`, `user`, `message`, `client`, `live`, `push`) — um sufixo técnico não torna `hotfix_config` ou `cupom_config` código, e nenhuma palavra de credencial é isenta; o vocabulário não cobre todo pedido possível (medido: 16 de 40 bloqueios novos de gate escalaram na 4ª revisão), e é por isso que o portão estrutural, não ela, é o controle — o prompt do respondedor também manda escalar esses assuntos; **falsos positivos aceitos** (falha fechado): "produção"/"prod"/"deploy" em outro sentido e qualquer menção a pagamento. É defesa em profundidade: o worker que agiria sobre a resposta não tem ferramenta de merge, deploy, cobrança ou mensagem, e o diff é verificado contra o write set antes de qualquer commit.
- **Escala como hoje** (decisão `clarification` pendente): `action: "escalate"`, `instructions` vazio ou só espaço, o run do respondedor falhar (`responder_run_failed`) ou sair sem uma resposta válida no schema (`responder_output_invalid`), `maxTimes` esgotado, ou a missão não ter `delegation.answerBlockers`. **Pausa ou cancelamento pelo fundador** (PIP-906 v2, achados 2 e 3 da 2ª revisão): se chegarem enquanto o worker ou o respondedor terminam — inclusive nos últimos `poll_seconds`, quando o processo já saiu `collected` —, a pausa do fundador vence: o respondedor não é chamado (ou sua resposta é descartada), nada é delegado, e a `clarification` fica pendente na missão `paused`; numa missão `cancelled` nada fica pendente. O supervisor nunca chama `pause` sobre uma missão já pausada, e uma pausa que chegue entre a checagem e o `open_run` do respondedor também termina em escalada, não em exceção.
- **Nunca delegável**: nada em `reservedToHuman`, os gates absolutos (`productionAllowed` etc.), nem nada que a guarda determinística cubra (lista acima) — o prompt do respondedor também instrui `escalate` para qualquer coisa fora do escopo da missão, mas isso é o modelo, não uma garantia; a guarda é o piso determinístico só para os termos que ela normaliza e casa, não uma checagem geral de `reservedToHuman` ou de escopo.
- Testes: `tests/mission/test_responder.py` (prompt, cerca dos bloqueios, comando, isolamento incluindo os `Read(...)` negados, parsing), `tests/mission/test_decisions_evidence.py::AnswerBlockersDelegatedDecisionTests` (store, inclusive a contagem independente de `maxTimes` entre as duas regras), `tests/mission/test_supervisor.py::AnswerBlockersDelegationTests`/`SensitiveTermsGuardTests`/`WriteSetBeforeResponderTests`/`PauseAndCancelDuringResponderTests`/`IndependentDelegationRulesTests` (ciclo completo, a guarda normalizada, o write set antes do respondedor, a pausa/cancelamento durante o respondedor e as duas regras juntas), além do contrato em `test_contract.py`.

## Store

`MissionStore(path)`; default `~/.pipe/mission/mission.sqlite3`. WAL, `busy_timeout`, recusa symlink, arquivos `0600`, `schema_version` em `metadata`. Tabelas: `missions`, `mission_events`, `mission_runs`, `decisions`, `criteria_evidence`. Custo acumulado = soma de `cost_usd` dos runs.

`mission_runs.executor` continua o texto histórico `"<papel>:<modelo>"` (`"worker:sonnet"`) — chamadores/testes antigos não mudam. Desde o PIP-911, `open_run` também grava `executor_kind` (`claude`/`local`; hoje sempre `claude`, default da coluna) e `model` (o modelo que rodou de fato) como colunas próprias, para responder "quanto rodou local" sem parsear `executor`. Banco em `schema_version` 2 ganha as duas colunas por migração (`_migrate_2_to_3`, `ALTER TABLE ADD COLUMN`, sem perder linha nenhuma): `model` é recuperado do que já existia depois do `:` em `executor`; `executor_kind` é sempre `claude`, porque nenhum outro executor existiu antes desta migração — não é um palpite. Testado em `tests/mission/test_store_migration.py::RunExecutorKindMigrationTests` (esquema 2 montado à mão, com e sem modelo no `executor`, migração dupla é no-op, e uma base ainda em `schema_version` 1 recebe as duas migrações em sequência).

Concorrência (follow-up A7 do PIP-901): supervisor e CLI são dois escritores no mesmo arquivo. Todo método mutante abre `BEGIN IMMEDIATE` antes de ler o estado que valida (`resume` inclusive), então um `pause` do fundador espera o lock em vez de comitar entre a checagem e a escrita do supervisor. Testado com duas conexões (`tests/mission/test_store_concurrency.py`).

## CLI

```text
pipe mission create <mission.json> [--store PATH] [--at RFC3339] [--json]
pipe mission show <MSN-id>
pipe mission status <MSN-id> [--home DIR] [--json]   # "Onde estamos / Por quê / O que depende de você"
pipe mission activate|pause|resume|cancel|complete <MSN-id> [--at ...]
pipe mission decisions <MSN-id> [--pending]
pipe mission decide <DEC-id> --option <opção> --by human:chat:<quem>

pipe mission supervise <MSN-id> [--detach] [--claude-bin claude] [--gh-bin gh] [--store PATH]
                       [--home DIR] [--poll-seconds 5] [--worker-model sonnet] [--reviewer-model sonnet]
pipe mission run-once  <MSN-id> [mesmas flags, sem --detach]
pipe mission reconcile <MSN-id> [--store PATH] [--home DIR] [--claude-bin claude]
```

Todos aceitam `--store` (default `~/.pipe/mission/mission.sqlite3`) e `--json`. `--home` é a raiz das pastas por missão (default `~/.pipe/mission`).

Códigos: `MISSION_CONTRACT_VIOLATION` e `MISSION_STATE_CONFLICT` saem com `READINESS_BLOCKED` (9); `MISSION_NOT_FOUND` com `INPUT_UNAVAILABLE` (4); `MISSION_SUPERVISOR_REFUSED` (cadeia inválida, outro supervisor vivo) e `MISSION_SUPERVISOR_ERROR` (falha de `git`/`gh`/processo) com 9. Mensagens são fixas e não ecoam o input.

## Supervisor: o fluxo de um ciclo

`run_once` executa **um** ciclo a partir do estado durável e devolve o próximo estado (`status` da missão + `reason`); `supervise` repete `run_once` até a missão ficar `completed`, `cancelled`, `unknown`, `paused` ou `blocked`, ou até um ciclo terminar esperando decisão (`pending_decisions`) ou um pedido de parada (`interrupted`). A ordem é fixa:

1. **Recusas**: a cadeia de eventos tem de verificar (`auditChainValid`) e nenhum outro supervisor vivo pode ser dono da missão (pid em `supervisor.pid`). Caso contrário, `MISSION_SUPERVISOR_REFUSED` e nada é gravado.
2. **Reconciliação**: run `running` sem supervisor vivo (o anterior morreu com SIGKILL, queda da máquina) vira `run.unknown`; abre decisão `escalation` (`stop`/`revise_mission`, padrão seguro `stop`) e a missão vai para `unknown` (terminal). **Nada é re-executado**: um run de resultado desconhecido pode ter tido efeito.
3. **Estado**: só missão `active` e sem decisão pendente segue.
4. **Orçamento**: custo acumulado = soma de `cost_usd` de todos os runs (worker **e** revisor), lido de `total_cost_usd` do JSON do Claude Code. O worker recebe `--max-budget-usd` = `maxBudgetUsd` − custo − reserva do revisor (US$ 1,50); se isso ficar abaixo de US$ 1,50, `budget.reached` + `mission.blocked` + decisão `budget`. Antes do revisor, o restante tem de ser ≥ US$ 1,50.
5. **Worktree**: `git worktree add --no-track <home>/<MSN-id>/worktree -b claude/<MSN-id>-<slug> <baseRef>` (idempotente; reusa a branch se existir). Um caminho existente só é reaproveitado se for o topo de um worktree de `workspace.repo` (mesmo `--git-common-dir`) na branch da missão; qualquer outra coisa com conteúdo (diretório dentro de outro repositório, worktree de outro repo ou de outra branch) → erro, nada roda. `--no-track`: sem ele, um `baseRef` remoto (`origin/main`) gravaria `branch.<nome-da-branch>.remote`/`.merge` na config **comum** do checkout principal — a mesma que `git_config_snapshot` (item 7) compara antes/depois do worker. Ver "Entrega sem hooks" abaixo.
6. **Worker**: `claude -p <brief> --output-format json --max-turns <maxTurnsPerRun> --max-budget-usd <restante> --permission-mode acceptEdits --permission-prompts none --allowedTools "Read,Edit,Write,Grep,Glob,Bash(git status*),Bash(git diff*),Bash(git log*),Bash(git show*),Bash(ls *),Bash(<cada check>)" --setting-sources project --strict-mcp-config --disallowedTools <deny-list> --append-system-prompt <regras fixas> --model <worker-model>`, stdin de `/dev/null`, contexto novo por ciclo (nunca `--resume`, nunca `--bare`). Ver "Isolamento do worker e do revisor" abaixo. O ambiente é herdado do supervisor (o `claude` autentica pelo login local); o supervisor não acrescenta segredo. Enquanto roda, o store é lido a cada `--poll-seconds`: `paused`/`cancelled` → SIGTERM ao grupo do processo (SIGKILL ao grupo após 10 s se o `claude` não sair) → `run.interrupted` → fim. Depois que o `claude` sai, por qualquer motivo (fim normal, pausa, timeout), o que restou do grupo recebe SIGKILL: um neto que ignora SIGTERM não sobrevive ao run.
7. **Config git**: antes de cada run do worker o supervisor tira um snapshot (só hashes) de `git config --local --list` e `git config --worktree --list` (quando o git aceita) no checkout principal e no worktree, e compara depois do run. Divergência → `mission.blocked` com `git_config_tampered` + decisão `escalation` (`stop`/`restore_and_resume`), sem verificar, revisar, commitar nem fazer push; o diff desse run nunca é entregue (um run coletado fica com `review.blocked`), então a retomada despacha um worker novo — restaure a config antes de retomar. Um worktree grava na config **comum** do checkout principal, e o supervisor roda `git commit`/`git push` com essa config (exceto `core.hooksPath`, sobrescrito na entrega; `core.sshCommand`, `remote.origin.url`).
8. **Coleta**: `session_id`, `total_cost_usd`, `num_turns`, `subtype` e contagem de `permission_denials` vão para o evento `run.*`; `subtype` de erro (`error_max_turns`, `error_max_budget_usd`…) ou resultado sem o JSON do worker → run `failed` com `reason`.
9. **Roteamento do worker**: `failed` → próximo ciclo (no último, `blocked` + decisão); `done:false` com `blockers` → decisão `clarification` + `paused` (padrão seguro `pause`), a menos que `delegation.answerBlockers` cubra o caso e o respondedor responda `instruct` (ver "O respondedor de bloqueios"), aí a missão segue `active` sem decisão pendente. **Negação de permissão não é veredito**: o ciclo segue para a verificação e o revisor como qualquer outro (na demo MSN-4a06360ef387 o worker fez o trabalho certo e teve `ls`/`node --test` negados). As chamadas negadas (`WebFetch`, `Bash(<comando>)`) e a lista de ferramentas permitidas só entram no arquivo de revisão se o ciclo terminar em `needs_revision`, depois das instruções.
10. **Verificação determinística** (antes de gastar revisor): HEAD tem de estar na branch da missão (senão `blocked` `branch_mismatch`); circuit breaker — mesmo `diffFingerprint` do ciclo anterior → `blocked` (`no_progress`) + decisão; arquivo fora do write set (`git diff --name-only --no-renames <baseRef>...HEAD` + `git status --porcelain --no-renames`: um rename conta origem **e** destino) → `verify.failed` + `review.needs_revision` **sem revisor**; critérios `check` (exit 0, teto 600 s) e `artifact` gravam evidência e, se algum falha, `needs_revision` sem revisor.
11. **Revisor** (contexto limpo): `claude -p` com a missão + diff, `--json-schema` do veredito, `--permission-mode plan`, `--allowedTools "Read,Grep,Glob,Bash(git diff *),Bash(git log *)"`, as mesmas `--setting-sources project --strict-mcp-config --disallowedTools <deny-list>` do worker, `--max-turns 20`, `--model <reviewer-model>`. Uma falha de infraestrutura (`reviewer_run_failed`: o run não terminou `collected`; `reviewer_output_invalid`: terminou mas sem um veredito válido no schema) não é um julgamento — ganha **uma** nova tentativa de revisão, sem novo worker, sobre o mesmo diff; se a segunda tentativa falhar do mesmo jeito, aí sim vira `blocked`. Um veredito `blocked` que o próprio revisor devolveu (run válido, JSON válido, `verdict: "blocked"`) nunca é repetido. Rubrics viram evidência. `satisfied` sem evidência de todos os critérios é rebaixado a `blocked`.
12. **Rotas**: `satisfied` → entrega; `needs_revision` → próximo ciclo com as instruções (no ciclo `maxCycles`: `blocked` + decisão); `out_of_mission` → `paused` + decisão `out_of_mission` (padrão seguro `pause`); `blocked` → `blocked` + decisão.
13. **Entrega** (`delivery.kind = pull_request`): imediatamente antes do commit do supervisor e de novo antes do push, HEAD tem de estar na branch da missão (`branch_mismatch`) e `changed_files` é refeito contra o write set (`delivery_outside_write_set`); qualquer falha → `blocked` + decisão (`stop`/`fix_and_resume`), sem commit, push nem PR. Depois: commit do que o worker deixou no worktree, `git push origin HEAD:refs/heads/<branch>` (publica o HEAD verificado, nunca a ref local pelo nome), `gh pr create` **uma vez por branch** (consulta `gh pr list --head` antes; o evento `delivery.pr_opened` também não se repete), e polling de `gh pr checks` a cada 30 s por até 60 leituras. `passed` → `delivery.checks_passed` → `complete()`; `failed` → `delivery.checks_failed` + `needs_revision` no próximo ciclo; sem resposta no teto → `blocked` (`delivery_checks_timeout`) + decisão `stop`/`keep_waiting`. Com `delivery.kind = none`, conclui após o revisor.

`complete()` continua exigindo, no store, evidência `satisfied` de todos os critérios, cadeia válida, nenhuma decisão pendente e, para PR, `checks_passed` depois do último `pr_opened`.

### Entrega sem hooks e sem vazamento de `GIT_*` (PIP-907, ampliado no PIP-905)

A entrega nunca executa código de hook de git — nem o que o worker pode ter deixado no seu próprio worktree, nem o hook padrão do repositório (`.git/hooks`) — e nenhum filho do supervisor (git, gh, checks, worker, revisor, respondedor) herda uma variável do ambiente do supervisor que mude o que o git faz ou qual interpretador Python roda:

- Todo git do supervisor contra o repositório da missão que poderia disparar hook — `worktree prune`/`worktree add` (em `ensure_worktree`: `post-checkout`, `reference-transaction`), o commit (`commit_if_needed`) e o push (`push_branch`) — roda com `-c core.hooksPath=/dev/null`: como `/dev/null` não é diretório, o git não acha hook nenhum, e ninguém consegue colocar um lá. `core.hooksPath` é relativo à árvore de onde o `git` é chamado; sem essa flag, um `git commit`/`git push` do supervisor com `cwd` no worktree da missão executaria `pre-commit`/`commit-msg`/`post-commit`/`pre-push` de dentro daquele worktree — código que o worker escreveu — com as credenciais do supervisor. Foi assim que a PIP-904 v1 alterou o repositório real (`core.bare=true`, branch movida) em 11/09. `--no-verify` não bastaria: ele pula `pre-commit`/`commit-msg`/`pre-push`, mas não `post-commit`, `post-checkout` nem `reference-transaction`. A verificação real continua sendo o diff/critérios/revisor da missão e, depois do PR, a CI (`requireChecks`) — a entrega não promete mais garantia do que isso.
- `child_env()` (em `delivery.py`, reaproveitado por `verify.py`) é o ambiente de **todo** filho git/gh do supervisor (worktree, snapshot, diff, commit, push, PR), de **todo** comando `check` de critério (`verify.run_check`) e do `claude` do worker, do revisor e do respondedor (o worker roda os checks pelo Bash). Ele parte do ambiente do supervisor e remove:
  - **toda variável cujo nome começa com `GIT_`** — um prefixo, não uma lista. Três revisões adversariais de listas de nomes falharam do mesmo jeito: a lista não tinha `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` (redirecionam a config inteira), `GIT_EXTERNAL_DIFF` (o git chama esse programa em vez de diffar — medido falsificando o diff que vai ao revisor), `GIT_SSH_COMMAND`, `GIT_PROTOCOL_FROM_USER`, `GIT_PAGER` nem `GIT_EDITOR`, e nenhuma delas aparece em `git rev-parse --local-env-vars` nem em `git config --list` (então a trava do item 7 também não as vê). Exemplos do que o prefixo cobre: o contexto de worktree vinculado (`GIT_DIR` absoluto e companhia, `GIT_CONTEXT_ENV`, que um hook exporta para o processo que lança), a config por ambiente (`GIT_CONFIG_PARAMETERS`, `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>`, `GIT_CONFIG_GLOBAL`…) e qualquer variável que uma versão futura do git inventar. O supervisor nunca precisa herdar uma: passa o que precisa explicitamente (`-c user.name`, `-c core.hooksPath`, `cwd`). `GIT_TERMINAL_PROMPT=0` é **forçado**, porque nenhum filho tem terminal para responder prompt;
  - `EDITOR`, `VISUAL`, `PAGER` e `SSH_ASKPASS` (`_PROGRAM_ENV`) — fazem o git ou o `gh` abrir editor ou executar programa, e nenhum filho é interativo;
  - o que um filho ainda precisa **fica**: `PATH`, `HOME`, as credenciais do `gh` (`GH_*`/`GITHUB_*`) e `SSH_AUTH_SOCK` (sem prefixo `GIT_`, então um push por ssh continua funcionando);
  - as variáveis do interpretador: `PYTHONPATH`, `PYTHONHOME`, `PYTHONSTARTUP`, `PYTHONUSERBASE`, `PYTHONNOUSERSITE`, `PYTHONPLATLIBDIR`, `PYTHONSAFEPATH`, `VIRTUAL_ENV`, `__PYVENV_LAUNCHER__` (`_INTERPRETER_ENV`) — um `PYTHONPATH=src` do supervisor rodando do código-fonte já quebrou um `pre-push` na demo de 11/09; as outras mudam qual interpretador/venv um filho que rode Python usaria.

  `PATH` e `HOME` continuam — são necessários para achar os binários e ler a configuração/credenciais legítimas do usuário. **Consequência para um critério `check`** (PIP-909): como nem `VIRTUAL_ENV` nem `PYTHONPATH` chegam ao filho, e o `check` roda dentro do worktree da missão — uma árvore separada do checkout principal, sem o `.venv` dele —, um `command` que precise dos pacotes de uma venv (`pytest`, o próprio pacote instalado em modo editável) nunca deve contar com uma venv "ativada" nem com um caminho relativo tipo `.venv/bin/python`: isso resolve para dentro do worktree, onde não há `.venv` nenhum. Chame o interpretador da venv pelo **caminho absoluto** (ex.: `command: "/Users/<você>/<repo>/.venv/bin/python3 -m pytest tests/mission -q"`). O comando de exemplo ao final deste documento usa `PYTHONPATH=src .venv/bin/python` (caminho relativo) só porque é para rodar manualmente a partir da raiz do checkout principal, nunca dentro do worktree de uma missão — não use esse padrão relativo num `command` de critério.
- `gh_env()` é `child_env()` mais `GIT_CONFIG_COUNT=1`, `GIT_CONFIG_KEY_0=core.hooksPath`, `GIT_CONFIG_VALUE_0=/dev/null` forçados por cima — usado por **todo** `gh` do supervisor (`delivery._gh`, o único ponto que invoca o binário `gh`: `existing_pr`, `open_pr`, `checks_status`). O `gh` roda `git` por dentro (por exemplo para resolver o `HEAD` antes de um `pr create`); sem essa variável, esse git interno herdaria hooks do repositório do jeito que um `git` direto do supervisor herdaria sem `-c core.hooksPath=/dev/null` — o mesmo risco do item acima, só que dentro de um processo que o supervisor não controla por flag de linha de comando.
- `ensure_worktree` cria o worktree com `--no-track` (item 5): com um `baseRef` remoto, sem essa flag a config **comum** do checkout principal ganharia `branch.<nome>.remote`/`.merge` — e duas missões criando seus worktrees ao mesmo tempo mudariam o snapshot uma da outra, disparando `git_config_tampered` (item 7) numa missão que não teve config alterada de verdade (foi o que aconteceu com o PIP-903 em 11/09). A trava em si não muda: uma mudança real de config durante o worker (`git config` rodado pelo worker, por exemplo) continua bloqueando com `git_config_tampered`.

### Decisões que o supervisor abre

| Situação | `kind` | Estado | Opções (padrão seguro primeiro) |
|---|---|---|---|
| run órfão | `escalation` | `unknown` | `stop`, `revise_mission` |
| orçamento | `budget` | `blocked` | `stop`, `revise_mission` |
| limite de ciclos (`max_cycles`, `needs_revision_limit`) | `escalation` | `blocked` (ou concedida sozinha — ver "Decisões delegadas" — e a missão continua `active`) | `stop`, `grant_cycle` |
| `no_progress`, `review_blocked`, `run_failed`, `delivery_checks_failed` | `escalation` | `blocked` (nunca delegável, mesmo com uma regra ativa) | `stop`, `grant_cycle` |
| checks sem resposta | `escalation` | `blocked` | `stop`, `keep_waiting` |
| config git alterada durante o worker (`git_config_tampered`) | `escalation` | `blocked` | `stop`, `restore_and_resume` |
| HEAD fora da branch da missão (`branch_mismatch`), diff fora do write set na entrega (`delivery_outside_write_set`) | `escalation` | `blocked` | `stop`, `fix_and_resume` |
| worker com `blockers` | `clarification` | `paused` (ou concedida sozinha pelo respondedor — ver "O respondedor de bloqueios" — e a missão continua `active`) | `pause`, `retry` |
| revisor `out_of_mission` | `out_of_mission` | `paused` | `pause`, `retry_within_mission` |

O supervisor não age sobre a opção escolhida, com uma exceção: cada decisão resolvida com `grant_cycle` (por um humano ou, dentro da regra declarada, por `delegated:orchestrator`) concede um ciclo além de `maxCycles`. Para continuar depois de uma decisão humana: `pipe mission resume <id>` e `pipe mission supervise <id> --detach`; uma decisão delegada já resume e segue sozinha, dentro do mesmo `supervise`.

## Pausar, cancelar, retomar

- `pipe mission pause <id>`: o supervisor vê o estado em ≤ `--poll-seconds` (padrão 5 s), SIGTERM ao worker, `run.interrupted`, e termina. Nada novo parte enquanto `paused`.
- `pipe mission resume <id>` + `pipe mission supervise <id> --detach`: o mesmo ciclo é retomado com `attempt` novo (a pausa não consome ciclo). Se a pausa veio durante o revisor, só a revisão é refeita.
- `pipe mission cancel <id>`: igual à pausa, mas terminal.
- SIGTERM/SIGINT/SIGHUP no próprio supervisor (`supervise` e `run-once`; SIGHUP chega quando o terminal de um `supervise` em primeiro plano fecha): termina o worker, registra `run.interrupted` e sai com a missão `active` (retomável).
- Supervisor morto com SIGKILL (ou queda): o worker roda em sessão própria e **continua vivo**, editando o worktree. `status` mostra `supervisor.alive=false` e um run `running`; `pipe mission reconcile <id> [--claude-bin <o mesmo do supervise>]` (ou o próximo `supervise`/`run-once`) primeiro encerra o worker (SIGTERM ao grupo, SIGKILL após 5 s) — só se o pid de `worker.pid` ainda for líder de sessão rodando `<claude-bin> -p`, para não matar um processo que herdou o pid —, depois marca o run `unknown` (`workerKilled` no evento) e abre decisão.

## Onde ficam as coisas

| O quê | Onde |
|---|---|
| Store | `~/.pipe/mission/mission.sqlite3` (0600, WAL) |
| Pid do supervisor | `~/.pipe/mission/<MSN-id>/supervisor.pid` — o mesmo arquivo que `status` lê para `supervisor.alive`; fica após a saída (então `alive=false`) |
| Log | `~/.pipe/mission/<MSN-id>/supervisor.log` — uma linha por evento, só ids, códigos e contagens; com `--detach`, stdout/stderr do supervisor também vão para ele |
| Worktree | `~/.pipe/mission/<MSN-id>/worktree`, branch `claude/<MSN-id>-<slug>` |
| Pid do worker em execução | `~/.pipe/mission/<MSN-id>/worker.pid` (removido ao fim do run) |
| Instruções de revisão do ciclo *n* | `~/.pipe/mission/<MSN-id>/revisions/cycle-<n>.md` (0600): texto do revisor, arquivos fora do write set, ferramentas negadas. Nunca no SQLite |

## O que nunca vai para o SQLite

Brief, prompt do revisor, texto de resultado do worker ou do revisor, diff e saída de comandos. O store guarda só ids (`sessionId`, `runId`), hashes (`resultFingerprint`, `diffFingerprint`), contagens, códigos (`reason`, `subtype`, modelo) e refs (URL do PR). Toda string de evento passa por `safe_identifier`; `collect_run(extra=...)` não aceita texto livre nem sobrescreve campos fixos. Testado varrendo o arquivo SQLite (e `-wal`) por uma sentinela do worker e do revisor. O `diffFingerprint` é o hash de um diff único da merge-base com `baseRef` até a working tree (mais arquivos não rastreados): commitar o mesmo conteúdo não o altera.

## Isolamento do worker e do revisor

O worker roda sem ninguém para responder a um pedido de permissão (`--permission-prompts none`), então o que ele pode fazer é só o que as flags liberam:

- **Sem as configurações do usuário**: `--setting-sources project` (as regras de allow e os hooks de `~/.claude/settings.json` — por exemplo `gh pr merge *`, `railway up *` — não se aplicam; `--allowedTools` **soma** às regras das fontes carregadas, não as substitui) e `--strict-mcp-config` sem `--mcp-config` (nenhum servidor MCP). Medido com o CLI real 2.1.267: sem isolamento o worker executou `gh pr review --help`; com as flags, negado, e a autenticação continua funcionando.
- **Pode**: `Read`, `Edit`, `Write`, `Grep`, `Glob`, `git status|diff|log|show`, `ls` e exatamente os comandos dos critérios `check`. Deixa as mudanças no worktree; quem commita é o supervisor, depois de verificar.
- **Não pode** (`--disallowedTools`; negar vence permitir, inclusive regras que viessem das settings do projeto): `gh`, `railway`, `git push|config|remote|-c|-C|checkout|switch|reset|worktree|commit|add|rebase|merge`, `curl`, `wget`, `WebFetch`, `WebSearch`. Qualquer outro comando também é negado (não está no allow). O brief e as regras fixas dizem isso ao worker.
- **O que o supervisor confere depois**: config git (snapshot antes/depois), branch do HEAD, write set sem detecção de rename, e de novo branch e write set antes do commit e antes do push.

O revisor recebe as mesmas flags de isolamento e o mesmo deny-list, com `--permission-mode plan` e allow só de leitura (`Read,Grep,Glob,Bash(git diff *),Bash(git log *)`). O respondedor (PIP-906) recebe as mesmas flags de isolamento, com allow ainda mais estreito — `Read,Grep,Glob`, sem nenhum `Bash` — porque ele só decide se a resposta já está no repositório, nunca inspeciona o diff, e um deny-list maior: além do deny-list comum, `Read` de `~/.ssh`, `~/.claude`, `~/.aws`, `~/.config/gh`, `~/.netrc`, `~/.npmrc`, `~/.pypirc` e de qualquer `.env` a partir da raiz (`responder.RESPONDER_DENIED_READS`) — porque `--setting-sources project` vale para os três (só as configurações do **usuário** ficam de fora), e é o único dos três cuja saída (`instructions`) vira o brief do próximo worker sem qualquer revisão. Ver "Write set antes do respondedor" acima para o que cobre o resto (um `.claude/settings.json` que o próprio worker tenha deixado no worktree).

## Limites

- **Sem merge automático.** O supervisor abre o PR e espera os checks; o merge é do fundador.
- **Sem Linear por código.** O handoff no Linear segue pelo MCP no chat (skill `/missao`).
- **Um escritor por vez.** Um supervisor por missão (pid); um worker por vez; nada de workers em paralelo.
- **Custo medido, não estimado**: `costUsd` é a soma de `total_cost_usd` reportado pelo `claude -p` de cada run. Run morto sem JSON conta US$ 0 (o custo real é desconhecido) e run reconciliado como `unknown` também.
- **Pid pode ser reutilizado** pelo sistema depois que o supervisor morre; se `status` disser `alive=true` sem supervisor, apague `supervisor.pid`. O `reconcile` só mata o pid de `worker.pid` se ele ainda for líder de sessão rodando `<claude-bin> -p`; o SIGKILL ao grupo depois que o líder sai tem a mesma janela teórica de reuso (o id de um grupo vazio pode voltar a existir).
- **Neto que sai do grupo escapa**: um processo que o worker inicie com `setsid` (sessão nova) não recebe o SIGTERM/SIGKILL do grupo na pausa, no timeout nem no `reconcile`. Não há solução simples no macOS; confira `ps -g <pgid>` depois de uma pausa se suspeitar.
- **`git diff` do allow aceita `--output=<arquivo>`** (grava um arquivo); dentro do worktree isso aparece no write set, fora dele depende das regras de escrita do Claude Code. Não verificado com o CLI real.
- **Só o pid do worker é gravado**: um revisor que fique vivo depois de um SIGKILL no supervisor não é encerrado pelo `reconcile` (ele é só leitura, mas gasta).
- **`unknown` é terminal**: continuar exige uma missão nova (`version` nova ou outro conteúdo).
- **Rede**: só a do supervisor (`git push`, `gh`), com as credenciais e a conta `gh` ativas da máquina. O worker não tem `gh`, `git push`, `curl`, `wget`, `WebFetch` nem `WebSearch`, mas herda o ambiente do supervisor; não rode o supervisor num shell com segredos exportados que o worker não deva ver.
- Evidência não gera evento próprio (allowlist fixa); fica em `criteria_evidence` e é lida por `status`/`complete`.
- O que o Pipe já governa (`AGENTS.md`, approval gates) continua valendo por cima.

## Testes

Só executáveis falsos (`tests/mission/fakes/fake_claude.py`, `fake_gh.py`); nenhum `claude` ou `gh` real. As falhas §5 do desenho têm teste nomeado em `tests/mission/test_supervisor.py` (`test_f1_…` a `test_f9_…`), mais circuit breaker, blockers, negação de permissão (não é veredito), PR idempotente, checks, sentinela fora do SQLite, escritor único, config git adulterada, rename de arquivo restrito, netos na pausa (inclusive o que ignora SIGTERM), `reconcile` com worker vivo (e o controle com pid de outro processo), branch e write set na entrega; o `--detach` real, o SIGHUP no `run-once` e o SIGKILL no `supervise` seguido de `reconcile` estão em `tests/mission/test_supervisor_cli.py`. As flags de isolamento e o deny-list são fixados literalmente em `test_worker.py`/`test_reviewer.py`.

"Entrega sem hooks e sem vazamento de `GIT_*`" (PIP-907) tem teste próprio: `tests/mission/test_delivery.py` (`ChildEnvGitContextTests`, e os testes de `commit_if_needed`/`push_branch` com hook plantado no worktree e no `.git/hooks` padrão, e o de `ensure_worktree` sem tracking na config comum), `tests/mission/test_verify.py` (`run_check`/`changed_files` ignoram `GIT_*` do ambiente) e `tests/mission/test_supervisor.py` (`GitConfigTamperTests` para a mudança real de config que continua bloqueando, e o cenário concorrente: outra missão criando seu worktree durante o run de um worker não dispara `git_config_tampered`).

"Ambiente limpo para todo filho do supervisor" (PIP-905) tem teste próprio em `tests/mission/test_delivery.py::ChildEnvIsolationTests`: 34 nomes `GIT_*` poluídos de uma vez — inclusive `GIT_CONFIG_GLOBAL`, `GIT_EXTERNAL_DIFF`, `GIT_SSH_COMMAND` e um `GIT_FUTURE_VARIABLE_NOBODY_HAS_SEEN_YET`, que prova que a regra é por prefixo e não por lista —, toda a saída de `git rev-parse --local-env-vars` poluída, `EDITOR`/`VISUAL`/`PAGER`/`SSH_ASKPASS`, `GIT_TERMINAL_PROMPT` forçado em `0`, cada variável do interpretador sozinha, e o que precisa sobreviver (`PATH`, `HOME`, `GH_TOKEN`, `SSH_AUTH_SOCK`). Em `tests/mission/test_supervisor.py::InheritedGitProgramEnvTests`, uma missão inteira roda com `GIT_CONFIG_GLOBAL` apontando para um gitconfig com `core.fsmonitor`/`core.pager` e com `GIT_EXTERNAL_DIFF`/`GIT_SSH_COMMAND` apontando para um script: a missão completa e o script nunca roda. O `gh` de cada chamada é conferido pelo log do fake (`FakeBinaries.gh_envs()`), e `tests/mission/test_verify.py` prova o mesmo para o comando de `check`.

"O respondedor de bloqueios" (PIP-906) tem teste próprio: `tests/mission/test_responder.py` (prompt, cerca `<<<BLOQUEIOS_DO_WORKER`/`BLOQUEIOS_DO_WORKER>>>` e o marcador neutralizado dentro de um bloqueio hostil, comando somente-leitura sem Edit/Write/Bash, os `Read(...)` negados para `.ssh`/`.claude`/`.env`, parsing da resposta), `tests/mission/test_contract.py` (limites de `answerBlockers` e a combinação com `grantCycle`), `tests/mission/test_decisions_evidence.py::AnswerBlockersDelegatedDecisionTests` (o store só resolve `clarification`/`retry` com `context.reason == "worker_blockers"`, dentro de `maxTimes`, nunca confunde a regra com `grantCycle`, e conta o `maxTimes` de cada regra separadamente) e `tests/mission/test_supervisor.py::AnswerBlockersDelegationTests`/`SensitiveTermsGuardTests`/`WriteSetBeforeResponderTests`/`PauseAndCancelDuringResponderTests`/`IndependentDelegationRulesTests` (o ciclo completo com o `fake_claude.py` distinguindo respondedor de revisor pelo `--json-schema`, a guarda determinística normalizada — acento, separador, caractere de largura zero, e as categorias de governança/comunicação externa —, o diff fora do write set barrando o respondedor antes de rodar, a pausa e o cancelamento durante o respondedor sem derrubar o supervisor, e as duas regras de delegação usadas na mesma missão contadas à parte). PIP-906 v2 (correção da revisão adversarial do PR #193, `tickets/review-pip-906.md`) é a fonte desses testes — cada um documenta o achado que corrige no comentário.

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -B -m unittest discover -s tests/mission -t . -q
```

## Program — uma dor em ondas de missões, com portão verificável entre elas (PIP-910)

A dor real: "a planilha de controle financeiro vira um sistema web" não é uma missão, são sete (esquema, dados, requisitos funcionais, requisitos não funcionais, protótipo, arquitetura, implementação em fases). Sem um objeto acima da missão, é o fundador — no chat — quem cria a próxima missão e confere se a anterior entregou: o gargalo que o Mission Loop existe para remover. Um `Program` fecha esse laço: encadeia ondas de missões com um portão mecânico entre elas, reaproveitando o supervisor de missão, o `MissionStore` e a verificação existentes por inteiro — sem duplicar nada e sem mergear nada (merge continua reservado ao fundador).

### O documento Program

`schemaVersion` "0.1.0" (`schemas/Program.schema.json`; contrato em `src/pipe_venture_builder/mission/program.py`), com:

- `programId` `PRG-<12hex>`, gerado por `stable_id` do conteúdo (como `missionId`); `fingerprint` cobre tudo menos `status`/`createdAt`/`updatedAt`/`fingerprint`.
- `objective`: a frase verificável da dor. `doneWhen`: critérios `check`/`artifact` do **programa** (pode ser vazia) — nunca `rubric`, porque é conferido por fora, mecanicamente, não por um modelo.
- `workspace {repo, baseRef}`: só a base; cada onda resolve a sua própria (ver "Encadeamento por branch").
- `constraints.maxBudgetUsd`: teto de custo acumulado do programa inteiro (soma do custo de cada missão de onda).
- `stages` (≥ 1), cada uma com:
  - `id`: slug único, tem de ser declarada depois de tudo que está no seu `dependsOn` (isso também recusa ciclo: um ciclo sempre precisa de uma referência para a frente).
  - `missionDraft`: um documento de missão **sem** `missionId`/`status`/timestamps/`fingerprint`/`program` e cujo `workspace` só declara `writeSet` — o programa resolve `repo` e `baseRef` na hora de criar a missão da onda.
  - `dependsOn`: ids de ondas já declaradas. `chainFrom`: obrigatório com duas ou mais dependências (tem de estar em `dependsOn`); com uma só, a dependência é o `chainFrom` implícito.
  - `startWhen`: critérios `check`/`artifact` (pode ser vazia) — o portão mecânico entre ondas, ver abaixo.
  - `requiresFounder`: portão humano opcional antes da onda.
  - `execution` ({`workerModel`, `reviewerModel`, `executor`} opcionais) — perfil de execução por onda. `executor ∈ {claude, local}` (PIP-911 onda 1) é o executor do **worker** da onda, gravado no contrato — hoje só ``claude`` roda de fato (o roteamento para um executor local por dentro do supervisor é a onda 2, ainda não implementada); uma onda cujo `missionDraft.successCriteria` tem qualquer critério `rubric` **recusa** `executor: "local"` na validação do contrato (`program._validate_execution`), porque um `rubric` é julgado pelo revisor, e o revisor não tem campo de executor algum — nunca é local (`supervisor._review`/`_answer_blockers` gravam `executor_kind: "claude"` sempre, sem ler nada da missão ou da onda). Testes: `tests/mission/test_program.py::ExecutorPolicyTests`.
- Toda missão criada por uma onda carrega `program: {"programId", "stage"}` (campo opcional do contrato `Mission`, ausente em toda missão que não veio de um programa — não entra na fingerprint de quem não o declara).

### Encadeamento por branch (decisão de desenho)

O programa **nunca mergeia** — merge continua reservado ao fundador. A onda 1 nasce de `workspace.baseRef` do programa; uma onda com dependência nasce da **branch da missão** da onda de `chainFrom` (`branch_name(mission)`, a mesma função que a entrega por PR já usa). Como uma missão com `delivery.kind: "none"` nunca comita (só a entrega por PR comita), o supervisor do programa comita o que sobrou no worktree da missão da onda — sem push, sem PR, nunca um merge — assim que ela termina `completed`, para que a próxima onda (ou o `doneWhen` final) veja o conteúdo por um `git worktree add` de verdade, não por um diretório de outra onda.

### O portão: `startWhen` por fora

`startWhen` não é "a missão anterior disse que terminou": é a mesma forma verificável de um critério de sucesso (`check` com exit 0, ou `artifact` com `mustMatch`), conferida **de fora**, num `git worktree --detach` descartável (removido depois) **da base resolvida da onda** — a mesma branch em que a missão dela vai de fato nascer, nunca no worktree da própria missão da onda seguinte. Insatisfeito → o programa fica `blocked`, uma decisão `escalation` abre (`stop`/`revise_stage`), e **nenhuma missão é criada** para a onda.

A identidade dessa base é o ponto inteiro do portão, e tem uma consequência que precisa ficar dita: como o programa **não mergeia**, uma onda com duas dependências enxerga só a de `chainFrom`. Um `startWhen` que cobra a entrega da *outra* dependência **bloqueia**, e isso está certo — aprovar o critério na branch dela deixaria a onda começar numa base onde aquele artefato não existe, que é exatamente o "construir sobre areia" que o portão existe para impedir. Para reconvergir duas ondas paralelas, ou elas são serializadas (`b` depende de `a`, `c` depende de `b`), ou o fundador mergeia. O `git` não inventa conteúdo, e o portão não finge que inventou.

### O laço (`pipe_venture_builder.mission.program_supervisor.supervise_program`)

No mesmo estilo de `supervisor.supervise` (repete até parar), por ciclo:

1. Programa não `active` → devolve o estado como está.
2. Custo acumulado (`store.program_cost_usd`) acima de `maxBudgetUsd` → `blocked` + decisão `budget` (`stop`/`revise_program`), sem iniciar outra onda.
3. Todas as ondas `completed` → avalia `doneWhen` contra as branches de **todas as folhas do grafo** (ondas de que ninguém depende), com um critério valendo se alguma folha o entregou: satisfeito → `program.completed`; senão → `blocked` + decisão `escalation` (`stop`/`revise_program`). Não é "a última declarada": ordem de declaração não é o grafo, e uma onda independente declarada por último não carrega nada da cadeia ao lado. Ao contrário do `startWhen`, aqui o "alguma" é correto — `doneWhen` só relata o que o programa produziu no total, e esse total fica espalhado pelas folhas.
4. Escolhe a próxima onda pronta: dependências todas `completed` (uma onda com uma missão em andamento — raríssimo, resume — é preferida a criar outra); sem isso, nada pronto → `active`/`waiting_on_dependencies`.
5. `requiresFounder`: decisão `approval` (`approve`/`stop`, padrão seguro `stop`) antes de criar a missão; enquanto pendente ou não aprovada, o programa fica `paused` e nada começa. Aprovada, o laço segue sem reabrir a decisão.
6. `startWhen` da onda (acima). Insatisfeito bloqueia sem criar missão.
7. Sem missão gravada ainda para a onda: resolve a base (encadeamento por branch), monta o documento a partir do `missionDraft` (+ `workspace.repo`/`baseRef` + `program`), `store.create` + `store.activate` + `store.record_stage_mission` (idempotente: gravar o mesmo par onda/missão duas vezes não faz nada; uma missão diferente para a mesma onda é erro de estado).
8. Roda o **supervisor de missão que já existe** (`supervisor.supervise`) sobre essa missão, do início ao fim — nada aqui reimplementa worker, verificação, revisor ou entrega.
9. Missão `completed` → comita o que sobrou no worktree da onda e volta ao passo 3. Missão `paused`/`blocked` → o programa segue para o mesmo estado, com uma decisão `escalation` própria (`stop`/`resume_stage`) — a onda seguinte nunca começa por baixo dela.

Retomar o laço (`supervise_program` de novo) nunca recria a missão de uma onda já `completed`, nem de uma onda cuja missão já existe: a leitura de estado é toda durável (`store.stage_mission`), nada fica na memória do processo.

### Decisões do programa: mesma tabela, `programId` como sujeito

`store.open_decision`/`pending_decisions`/`resolve_decision` aceitam tanto `MSN-` quanto `PRG-` como sujeito — nada muda para missão. Os eventos de uma decisão de programa (`decision.opened`/`decision.resolved`/`decision.delegated`) ficam encadeados em `program_events`, não em `mission_events`: `_subject_kind`/`_append_subject_event` decidem a tabela pelo prefixo do id. Delegação (`delegated:orchestrator`) continua exclusiva de missão — nenhuma decisão de programa é resolvida sozinha.

### Store: `programs`, `program_events`, `program_stage_missions`

Mesma disciplina do `MissionStore`: `create_program`/`get_program`/`activate_program`/`pause_program`/`resume_program`/`cancel_program`/`block_program`/`complete_program` (transições fixas, como `TRANSITIONS` de missão); `record_stage_mission`/`stage_mission` (idempotente); `program_cost_usd` (soma de `total_cost_usd` de cada missão de onda gravada); `list_program_events`/`verify_program_chain` (cadeia por hash própria, como a de missão — `build_program_event`/`verify_program_event` em `events.py`, allowlist `PROGRAM_EVENT_TYPES` separada de `EVENT_TYPES`, que continua fixa e sem `program.*`). Payload de evento carrega só id e código, nunca o texto do objetivo ou da intenção.

### `pipe program` — mesmo padrão de `pipe mission`

```text
pipe program create <program.json> [--store PATH] [--at RFC3339] [--json]
pipe program show <PRG-id>
pipe program status <PRG-id> [--home DIR] [--json]
pipe program activate|pause|resume|cancel <PRG-id> [--at ...]
pipe program decisions <PRG-id> [--pending]
pipe program supervise <PRG-id> [--detach] [--claude-bin claude] [--gh-bin gh] [--store PATH]
                       [--home DIR] [--poll-seconds 5] [--worker-model sonnet] [--reviewer-model sonnet]
```

`program status` devolve `programId`, `status`, `objective`, `costUsd`, `auditChainValid`, `pendingDecisions` e `stages` (cada uma com `id`, `status`, `missionId`, `reason`). Registrado por `register_program_commands`, chamado de dentro de `register_mission_commands` — o `pipe program ...` de nível superior não exige tocar no CLI raiz do Pipe.

### O que não muda

O que é delegável continua o do PIP-903/906 (nunca ampliado por um programa); cada onda continua entregando pela mesma missão fiscalizada (write set, verificação, revisor, PR quando `delivery.kind` da onda for `pull_request`); o programa em si nunca abre PR nem comita para publicar — só a persistência interna descrita acima, sem push. Testes próprios de cada regra (escolha da onda, encadeamento por branch, `startWhen`, portão humano, missão parada, teto de orçamento, `doneWhen`, idempotência) em `tests/mission/test_program.py`; os achados da revisão adversarial em `tests/mission/test_program_review.py` (hooks desligados no `worktree add`, base do `startWhen`, folhas do `doneWhen`, atomicidade de `create`/`activate`/`record`); a superfície de linha de comando em `tests/mission/test_cli_program.py`; e a migração de banco existente em `tests/mission/test_store_migration.py`. Mais os aceites ocultos do PIP-910.
